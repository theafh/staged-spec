# spec_implement

Fully implement this spec with every detail as described here in the end double check between your implementation and the spec, run all tests and verify no errors or warnings exist. Fix until all details of the spec are implemented and code runs without any issues.
